{
  "installCommand": "npm install && npm install --prefix client && npm install --prefix server",
  "buildCommand": "npm run build --prefix client && rm -rf public && mkdir -p public && cp -r client/dist/. public/",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api?route=$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}